const ADDED = 'Added';
const CHANGED = 'Changed';
const REMOVED = 'Removed';

const changeTypes = [ADDED, CHANGED, REMOVED];

const UNRELEASED = 'Unreleased';

module.exports = { changeTypes, UNRELEASED, ADDED, CHANGED, REMOVED };
